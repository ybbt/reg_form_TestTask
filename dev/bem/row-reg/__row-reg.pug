mixin rowReg(rowTitle, inputFlag, inputType)
	.row-reg
		div.row-reg_title= rowTitle
		if (inputFlag== true)
			input.row-reg_input(type=inputType)
		else
			select.row-reg_input(name=rowTitle)
				- for (var i = 1950; i<=2020; i++)
					option(value=i)= i